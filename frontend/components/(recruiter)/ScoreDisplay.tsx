"use client";
// Aggregate score visualization primitives — re-exported from the UI library so
// callers don't have to pick between component packages.
export { ScoreRing, ScoreBar, ScorePill } from "@/components/ui";
