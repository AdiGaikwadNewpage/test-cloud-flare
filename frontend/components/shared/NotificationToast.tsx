"use client";
// Re-export the toast hook from the UI primitives so consumers can import
// notifications from `@/components/shared` consistently.
export { ToastProvider, useToast } from "@/components/ui/toast";
